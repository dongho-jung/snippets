# find foobar in entire commit history
git grep foobar $(git rev-list --all)

# upgrade submodule
git submodule update --recursive --remote

# git clone https with ssh key
git config --global url."git@github.com:".insteadOf "https://github.com/"
